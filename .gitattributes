# 資料與網頁資源在 working tree 一律 LF，parser 只認 \n
# （Windows autocrlf 會把 md 轉 CRLF -> frontmatter 解析壞掉）
*.md text eol=lf
*.json text eol=lf
*.js text eol=lf
*.css text eol=lf
*.html text eol=lf
# workflow 的 run: 是 bash，CRLF 會讓每一行結尾多一個 \r 直接跑不動
*.yml text eol=lf
*.yaml text eol=lf

# .bat 必須是 CRLF（Windows 要求）
*.bat text eol=crlf

# 圖片當 binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
