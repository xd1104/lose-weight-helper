# lose-weight-helper（減重助手）— 專案備忘（給接手的 AI／開發者）

手機優先的每日熱量記錄 PWA。**兩個人共用一份 app（Benson 與女友），資料完全獨立。**
吃完用講的或拍一張照，Claude 估熱量，一眼看出今天有沒有超過 TDEE。
架構刻意比照 `travel-book`（同一位作者的旅遊 PWA），兩邊的慣例一致，改動前先看那邊有沒有既有做法。

## 多使用者（v2 拍板，別自作主張合併）
- **完全獨立**：紀錄、TDEE、目標、常吃清單全部各自一份，**刻意不共用、不互看**。
  曾評估過「常吃清單共用省 API 錢」與「熱量互看有激勵效果」，Benson 拍板都不要。要改回來需要他再拍板。
- **Netflix 式切換**：進 app 先選人（`picking=true` 的全螢幕 picker），選過之後這台裝置記在
  localStorage `lwh_user`，之後直接進那個人的畫面；點右上頭像回到選人畫面。
- `me` 是目前使用者物件，`users` 是名冊。**每一個 STORE 呼叫都要帶 `me.id`**。
- **切換使用者時整個 `db` 打掉重建**（`switchUser`），不可沿用上一位的 `days` 快取，否則會看到別人的紀錄。
- 非同步載入都有 **`if(me.id!==u) return` 的守門**：切人切很快時，上一位的回應不可以蓋掉現在這位的畫面。
- `persistChains` 的 key 一律前綴 uid，兩個人的寫入不會排進同一條鏈。
- **AI key 與 GitHub PAT 綁「裝置」不綁使用者**（同一支手機兩個人共用一把），這是刻意的。

## 架構（三層，比照 travel-book — 別打破）
- **電腦本機 Node App＝真本**：`server.js`（零執行期依賴，**port 3619**），服務 `public/` 前端＋`/api` CRUD，資料存本機 md 檔。
- **GitHub repo `xd1104/lose-weight-helper`（main）＝同步中樞＋雲端備份**：本機寫入後自動 `git add/commit` → `pull --no-rebase -X ours` → `push`；啟動時也 pull。
- **GitHub Pages（main `/docs`）＝手機 PWA**：`build.js` 從 `public/` 鏡射到 `docs/`。**docs/ 是產物，別手改**。

## 前端 DataStore（`public/store.js`；依 `location.hostname` 自動切；`?store=github` 可強制測 GitHub 模式）
- **localhost → LocalStore**：打本機 `/api`，全功能。
- **非 localhost（Pages）→ GitHubStore**：**有 PAT** 走認證 Contents API 讀寫（即時；PUT/DELETE 帶 sha、base64、409/422 自動重取 sha 重試一次）；**無 PAT** 唯讀走 raw＋`?t=<now>` cache-buster。
- **刻意「按日期取檔」而不是列整個 days 資料夾**：列資料夾的請求數會隨著使用月數線性成長，手機上會越用越慢。首頁只載入「當日 + 前 6 天」，歷史頁才另外抓 index＋最近 30 天。改回列全部＝效能退步。

## 同步（別誤改的決策）
- 同檔衝突：**固定電腦版本勝（`-X ours`）**——沿用 travel-book「電腦是真本」的選擇。
- 同步失敗絕不影響本機存檔（只記 log）。`AUTO_SYNC=0` 可關（測試用）。
- **`initSync()` 會先檢查 `ROOT/.git` 存在才啟用**：這道守門是刻意的。本專案曾被放在別的 repo 的子資料夾裡暫存，沒有它會把那個 repo 整包 `add -A` / push 出去。

## 金鑰（安全）
- **Anthropic API key**：存 localStorage（key **`lwh_anthropic_key`**），只在該裝置。前端直連 `api.anthropic.com`，**必須帶 `anthropic-dangerous-direct-browser-access: true`**，否則 CORS 擋掉。
  - 這是明知的取捨：PWA 在 Pages 上沒有後端，而「在外面吃飯當下就要知道熱量」不能依賴家裡電腦有沒有開機。要降風險就到 console 設每月花費上限。
  - **絕不寫進程式、絕不 commit。**
- **GitHub PAT**：fine-grained、只授權 `lose-weight-helper` 一個 repo 的 Contents 讀寫，存 localStorage（key **`lwh_gh_pat`**）。設定入口只在非 localhost 顯示。

## 資料格式（定案；前後端各有一套 mirror parser，改要一起改）
- **檔案佈局**（v2 起每個人一個資料夾）：
  ```
  data/users.md                              名冊：## 使用者 下每行 {id,name,emoji,color,createdAt}
  data/users/<uid>/profile.md                身體資料／目標／模型
  data/users/<uid>/foods.md                  常吃清單
  data/users/<uid>/days/YYYY-MM-DD.md        每天一個檔
  ```
  - `<uid>` = `<ts36>-<slug(名字)>`。**`safeName` 與前端 `slugify` 必須是同一個字元集（`\p{L}\p{N}` ＋ `._-`）**——
    不一致時中文／日文名字會在兩端被 mangle 成不同資料夾，同一個人跨裝置分裂成兩份資料（travel-book QA B1 的教訓，測試裡有專門一條在守）。
  - 名冊裡 **id 重複只留第一個**（`normalizeUsers`），避免兩個人指到同一個資料夾。
  - 刪除使用者＝連同整個資料夾刪掉；GitHubStore 沒有「刪資料夾」這種 API，所以 `_deleteTree` 遞迴逐檔刪、**刻意序列化執行**（平行刪同一棵樹很容易撞 409）。
  - **v1→v2 遷移**：server 啟動時若看到舊的 `data/profile.md` 而沒有 `data/users.md`，會把單人資料搬進一位叫「我」的使用者。冪等，`users.md` 存在就完全不動。
- **每天一個 `days/YYYY-MM-DD.md`**（檔名就是唯一 key；server 的 `safeDate` 只收嚴格 `YYYY-MM-DD`，順便擋 path traversal）。
  - frontmatter：`date/weight/updatedAt`（字串 JSON-quoted、數字裸寫）＋三段 body：
  - `## 飲食` → 每行 `- {id,time,meal,name,kcal,p,c,f,portion,note,src}`（key 順序固定、空值不寫）
    - `meal`：`breakfast|lunch|dinner|snack`，未知值一律落到 `snack`（**不可讓資料消失**）
    - `src`：`ai|manual|preset`（保留來源，之後才能回頭檢討 AI 估算準度——別當死碼清掉）
  - `## 運動` → 每行 `- {id,time,name,kcal}`
  - `## 備註` → **永遠最後一段、整段原樣文字**（parser 進入後不再解析 heading，所以備註裡打 `##` 不會壞）
- **`profile.md`**：frontmatter `sex/age/height/weight/activity/tdee/goal/model`。
  - `tdee` = 0 表示「用 Mifflin-St Jeor 自動算」，>0 = 手動覆寫。`goal` 是每日加減（負數＝減脂缺口）。
  - `cleanProfile` 會把離譜數值夾回合理範圍（活動係數超出 1–2.5 落回預設），避免算出負的熱量目標。
- **`foods.md`**：`## 食物` 下每行 `- {id,name,kcal,p,c,f,portion,n}`。`n` = 用過次數，是「常吃」清單的排序依據。
  - **這是省錢機制**：AI 算過一次就記起來，同樣的東西下次直接點，不用再花 API 錢。上限 200 筆。**兩個人的清單是分開的**（拍板的決定）。
- **一整天被清空 → 直接刪檔**（不留空殼 md）。GitHubStore 那邊對應 `_deleteFile`。
- **`.gitattributes` 強制 md/js/css/html/json 為 LF**；前後端 parser 開頭都先 `replace(/\r\n/g,'\n')`。壞的 JSON 行 parser 會跳過該行（不整檔炸掉）。

## AI（`public/ai.js`）
- 模型可選 Sonnet 5 / Opus 5 / Haiku 4.5，存在 profile（跟著同步）。**預設 Sonnet 5**：這是估算題不是推理題，Sonnet 的準度夠而成本約 Opus 的 6 折。要更準就在設定切 Opus 5。
- **用 structured outputs（`output_config.format` + json_schema）**，不是叫模型「回 JSON」再自己 parse 自由文字。`effort: "low"`（簡單估算，不需要深度推理，省時間也省錢）。
- **`max_tokens: 4000` 是含 thinking 的**，調小會出現估到一半被截斷。
- 回傳每個食物都帶 `portion`（份量假設）與 `confidence`。**UI 一定要把 portion 顯示出來**——使用者要能一眼看出 AI 是不是份量抓錯，這是這個 app 可信度的關鍵。
- 結果一律先進「可編輯的預覽」，使用者確認才寫入。**不要改成直接寫入**。
- 照片先壓到長邊 1024px / JPEG 0.85 再送。手機原圖 4000px 又慢又貴（圖片 token 隨解析度增加），1024px 對「這盤是什麼、大概多少」已經非常夠。
- **不存原始照片**：資料在 git repo 裡，塞 base64 照片會讓 repo 迅速膨脹。只留 AI 判讀結果。
- 本機用量／花費估算存 localStorage `lwh_ai_usage`（只是估算顯示用，帳以 Anthropic console 為準）。

## 體重（v2.1）
- `day.weight` 早就在資料格式裡，但之前**沒有 UI 入口**——一個叫「減重助手」的 app 不能記體重是最大的缺口，v2.1 補上。
- 首頁熱量環正下方一張體重卡（點進去輸入），歷史頁有趨勢折線＋區間增減。
- **記今天的體重會一併寫回 `profile.weight`**：TDEE 是用體重算的，不同步會越用越不準。UI 上有明講，別偷偷拿掉。
- 趨勢折線**只連有量到的日子，不補值**——補值會畫出一條看起來很順但假的曲線。
- 全部一樣重時 span 夾在 0.6，避免折線貼著邊框。
- 歷史頁「只量體重、沒記飲食」的日子熱量顯示 `—` 而不是 `0`（0 會被誤讀成「今天沒吃」）。

## `updatedAt` 的語意（踩過的坑，別改回去）
- `cleanProfile` **讀取時原樣保留 `updatedAt`**，時間戳只在 `serializeProfile` 落檔那一刻蓋。
- 曾經在 `cleanProfile` 裡無條件蓋 `new Date()`，結果 `updatedAt` 變成「剛才讀檔的時間」，
  前端「還沒設定過身體資料」的引導**永遠不會出現**（QC 實測抓到）。測試裡有一條專門釘住這件事。
- 前端存檔時會自己把 `db.profile.updatedAt` 標記起來（代表「這個 session 設定過了」），這樣不用重讀檔就能把引導收起來。

## 唯讀模式的出口（踩過的坑，別移除）
- 手機端（Pages）沒貼 GitHub 金鑰＝唯讀。**選人畫面一定要留一個「貼上金鑰」的出口**（`openKeysSheet`）。
- 曾經是死路：沒有使用者 → 選人畫面只有「新增使用者」→ 被唯讀擋掉 → 而「設定」要有使用者才進得去
  → **完全沒有地方能貼金鑰，手機第一次開就卡死**。
- `openKeysSheet()` 刻意**不依賴 `me`**（選人畫面時 `me` 是 null），裡面同時放 GitHub 金鑰與 Anthropic API key。
- 唯讀時直接**不顯示**「新增使用者」磚（按了也沒用，不如不給），文案也要跟著換——
  不能在建不了使用者的狀態下叫他「先建立第一位使用者」。

## 新使用者引導
- 建完使用者**直接跳出身體資料設定 sheet**：不設的話首頁的目標是用預設值（男/30歲/170cm/65kg）算的，等於假的。
- 沒設定過的人首頁上方有黃色提醒卡；設定完自動消失。
- 設定 sheet 裡改 chip 前會先把已輸入的數字讀起來（`readInputs`），否則重畫會把使用者剛打的身高體重吃掉。

## 運動 AI 估算（v2.2）
- 運動的消耗跟**體重高度相關**（食物不會），所以 `aiAnalyzeMove` 一定要把 profile 的體重／年齡／性別帶進 prompt。
- **一定要 AI 回「淨消耗」**（`(MET-1) × 體重 × 時數`）：TDEE 的活動係數已經含了那段時間的靜息代謝，
  一般 app 報的是總消耗，直接拿來扣會系統性高估、變成「怎麼練都瘦不下來」。prompt 裡寫死了，別拿掉。
- 回傳的 `detail` 一定要顯示出來（時長／強度／MET／體重）——同「食物要顯示 portion」的理由：
  使用者要能一眼看出 AI 是不是抓錯強度。
- 數字填進表單後**仍可手改**才存，跟食物一樣不直接寫入。
- **快速選擇＝你做過的運動（從 `db.days` 撈）＋內建預設**，刻意不另外開檔案存：資料本來就在 days 裡。
  自己做過的用 `.chip.mine` 標示，跟內建的區分開。

## 熱量目標的語意與基礎代謝下限（v2.4）
- 首頁那個數字是**上限**不是要達成的目標。減脂時（`goal<0`）標籤顯示「每日上限」，
  維持／增肌時才顯示「每日目標」——Benson 問過「這是要達到還是不能超過」，
  原本一律叫「目標」會讓人以為要吃到。
- **真正該看的是「缺口佔 TDEE 的百分比」，不是跟 BMR 比**。「不能吃低於基礎代謝」是常見的
  經驗法則、不是硬性生理禁令；設定頁固定顯示缺口百分比與理論每週減重，>25% 給黃色提醒。
- **目標低於 BMR 時，設定頁一定要警告**（紅色）。久坐（1.2）＋ 減脂快（-500）很容易讓目標掉到 BMR 以下
  （Benson 的實際設定就是：BMR 1720、TDEE 2064、目標 1564）。長期吃低於基礎代謝，
  掉的會有一大部分是肌肉。警告會算出「缺口要縮到多少以內」給具體解法。
- 首頁「今天吃太少」的提示**只在過去的日子或今天 20 點後才顯示**——一天還沒過完就跳
  「低於基礎代謝」很煩，會被無視。

## 營養分頁（v2.3）
- 三大營養素目標的**順序是固定的**：先蛋白質（體重決定）→ 再脂肪（熱量百分比）→ 碳水拿剩下的。
  這樣「碳水超標」自然等於「另外兩個沒吃夠」，語意才通。
- **蛋白質與脂肪／碳水的判定語意不同**，`macroRow` 的 `mode` 就是在區分這個：
  - `atleast`（蛋白質）：吃不夠才是問題，超過不算壞事
  - `cap`（脂肪／碳水）：超過才是問題；脂肪另有下限 `體重×0.6`，長期低於會影響荷爾蒙
- 蛋白質缺口 > 目標 60% 時**刻意不給零食換算**——那不是「補一下」的量，
  講「等於 5 份高蛋白飲」只會讓人放棄，改成提醒正餐要配蛋白質。
- 食物換算同一種最多列 2 份就換下一種（三杯高蛋白飲不是實際會做的事）。
- 三大營養素加總的熱量與記錄熱量差超過 15% 時會提示——通常是某幾筆手動輸入只填了熱量沒填營養素。
- 首頁的三大方塊點下去會跳到營養頁（`data-nav2`）。

## 熱量計算（定案）
- BMR 用 **Mifflin-St Jeor**（目前公認誤差最小）：男 `10w+6.25h-5a+5`、女 `10w+6.25h-5a-161`。
- TDEE = BMR × 活動係數（1.2／1.375／1.55／1.725／1.9），`profile.tdee>0` 時覆寫。
- 每日目標 = TDEE + goal，下限夾在 800。
- **活動係數＝「不含運動」的日常生活強度**，UI 的說明刻意寫「工作型態與走路量」而不是教科書的「每週運動幾次」。
  教科書定義把運動算進係數裡，但本 app 另外有運動欄位；照教科書選就會把健身房算兩遍、目標虛高。
  設定頁與首次引導都有一句提醒，**別把它拿掉**（Benson 問過「輕度是什麼意思」才發現原本文案在引導重複計算）。
- **活動係數＝「不含運動」的日常生活強度**，UI 說明刻意寫「工作型態與走路量」而不是教科書的「每週運動幾次」。
  教科書定義把運動算進係數裡，但本 app 另外有運動欄位；照教科書選就會把健身房算兩遍、目標虛高。
  設定頁與首次引導都有一句提醒，**別拿掉**（Benson 問「輕度是什麼意思」才發現原本文案在引導使用者重複計算）。
- **淨攝取 = 吃進去 − 額外運動**。活動係數已含日常活動，所以「運動」欄位只記額外運動；UI 上有寫明，別拿掉那句提示，會造成重複扣抵而低估。

## 讀取／歷史頁（2026-07 稽核後定案，別改回去）
- **讀日紀錄失敗 ≠ 那天沒吃**：`ensureDays` 先放空殼避免重複請求，但**失敗時一定要把空殼收回來**（只收回「還是原本那個空殼」的，載入途中記的東西不能弄丟）。留著的話 `db.days[k]` 有值 → 永遠不再重試，而那個空白的一天一旦被存回去就會蓋掉雲端真正的紀錄。同理 GitHubStore 讀 day 走 **`_readTextStrict`**（只有真 404 才回 null，其他錯誤往外丟）——別為了「比較不會噴錯」改回寬鬆的 `_readText`。
- **歷史頁的天數用單一常數 `HIST_DAYS`**（載入與繪製共用）。原本載 30、畫 60，第 31–60 天永遠顯示「—」。
- **`histDates` 由 `persistDay` 順手維護**：有內容就加進去、整天清空就移除（判斷條件 `dayHasData` 要跟 server 的「空天刪檔」條件一致）。歷史清單一輩子只在開 app／第一次點歷史時抓一次，不維護的話當天新記的紀錄要重開 app 才看得到。

## 寫入與同步（2026-07-26 掉資料事故之後定案，別退回去）
- **手機版（GitHubStore）所有寫入共用一條佇列**（`chainPersist` 的 lane 在 `!STORE.local` 時固定是 `"gh"`）。Contents API **一次 PUT ＝ 一個 commit**，衝突發生在**分支**層級不是檔案層級——不同檔案平行寫一樣會 409。本機版直接寫檔沒這問題，維持 per-file 排隊。
- **`rememberFood()` 只動記憶體、不寫檔**，寫檔由呼叫端（`addEntries`）整批做一次。以前每筆都寫，一次記 6 筆＝6 次 foods 寫入＋1 次 day 寫入＝7 個請求互撞，真的掉過一整餐。
- **`_putFile` 對 409/422 退避重試 3 次**（300/600/1200ms）。409 有兩種：檔案 sha 過期（重抓就好）、分支剛被別的 commit 動過（重抓沒用，要等對方落地）。以前只立刻重試一次，第二種常常第二次又撞。
- **待同步佇列 `pending` ＋ 畫面上的 `#sync-bar` 橫幅**：樂觀更新的代價是「畫面說已記錄、雲端其實沒有」。寫入失敗不能只跳 toast 就放棄，要留著、回到前景／`online`／按「立即重試」時再送，橫幅在真的寫進去之前不會消失。
- **persist* 一律送快照**（`JSON.parse(JSON.stringify(...))`）不送 live 物件：重送時 `db` 可能已經切成別的使用者了，送 live 物件會把 A 的資料寫進 B 的檔案。
- 測試：`test/browser/12-sync.js`（寫入次數、序列化、409 退避、橫幅、重送後資料不掉）。

## 首頁熱量卡＝兩層（v2.7，別攤回一張）
- **收合（預設）只放每天真的要看的三件事**：圓環（還能吃／超了多少）、已攝取（有運動才加一行運動消耗）、一條三色比例條＋一句白話。全部攤開時一張卡上有九個數字，重點反而看不出來。
- **展開（`showDetail`，點卡片上半部切換）才是解釋用的數字**：每日上限、維持體重（TDEE）、淨攝取。三大營養素**固定放在收合的卡上**（每天都要看），明細裡不重複放。
- **三大營養素就是三個方塊寫「目前／目標 g」，別再換成「熱量來自哪裡」的三色比例條**——那回答的是另一個問題，Benson 直接反映看不懂。方塊唯一缺的是超標看不出來（bar 卡在 100%），所以補了 `.macro.over`（琥珀底＋「超標」標籤）。蛋白質門檻 1.2 倍、碳水脂肪 1.05 倍。
- **正常範圍時不顯示提示條**：「還在額度內，剩 X」跟圓環中央是同一句話，講兩次只佔版面。只有「超過上限」「快到上限」「低於基礎代謝」才出現。
- **`macroVerdict` 的蛋白質不足只在 `lateEnough`（晚上 8 點後或看過去的日子）才講**，跟「吃太少」那條同一個判斷；還沒到就中性地報比例（順便當彩色條的圖例）。蛋白質超標門檻放寬到 1.2 倍——吃多一點不是問題。
- 測試：`08-macros.js`[1]、`09-floor.js`[1][4][5][5b]（含「收合時不該出現 TDEE／公克數」的反向斷言）。

## 三大營養素允許小數（v3.7）
- **`round1`（前後端鏡像）＝保留一位小數**，`cleanEntry` / `cleanFood` 的 p/c/f 都用它。食品標示常常是「蛋白質 2.5 g」，全部四捨五入成整數，一天記十筆就累積出可觀誤差。**熱量維持整數**（0.5 大卡沒有意義）。
- **所有營養素輸入框一律 `inputmode="decimal"` ＋ `step="0.1"`**。原本是 `inputmode="numeric"`——**iOS 的 numeric 鍵盤沒有小數點鍵**，根本打不進去（Benson 回報）。要新增營養素欄位時別再複製 numeric 那一版。
- **顯示用 `gram()`**：2.5 要看得到，25 不要變成 25.0。
- 測試：`test/roundtrip.js`（小數 round-trip、12.3456 只留一位、熱量仍是整數）、`test/browser/15-fav-coach.js`[A4e]（含 `inputmode`／`step` 的斷言）。

## 常吃清單的 `star` 釘選（v3.6）
- **問題**：記過的東西一律自動進常吃清單，兩週就 40 筆、其中 30 幾筆只吃過一次（芒果、羊肉片…），真正天天吃的無糖豆漿被淹掉。所以「常吃」其實是「吃過」。
- **`star`（foods 的新欄位，前後端鏡像）＝使用者主動釘選的**。清單分「★ 常吃」與「吃過的」兩區，釘選的排最上面。**版面成本只有一條分區標題**（Benson 明確要求別再變亂）。沒釘選的不寫這個 key，舊檔相容。
- **釘選入口刻意有兩個，都不佔列上的空間**：
  1. 今天頁點某一筆飲食 → 編輯 sheet 裡的「☆ 加入常吃清單」（`#e-star`）。**營養素直接沿用那一筆**，這是「自己新增很麻煩、還要記碳水蛋白質」最直接的解法。同名的更新並釘選，不會長出第二筆。
  2. 常吃項目的編輯 sheet 裡的「☆ 釘選為常吃」（`#fd-star`）。
- **新增／編輯常吃項目時的「🤖 讓 AI 幫我填數字」（`#fd-ai`）**：打完名稱按一下，`aiAnalyzeOne` 回來自動填熱量與三大營養素（沒填名稱不會白打）。

## 常吃清單＝可編輯的多選清單（v3.4）
- **點整列＝勾選**（`favPick`），可以一次勾好幾樣、最後按一次「加入 N 筆」。以前一次只能加一筆，早餐固定三樣就要開三次 sheet。
- **點 ✎（`data-fav-edit`）＝編輯**：名稱／份量／熱量／三大營養素，**刪除收在編輯 sheet 裡**（一列不塞兩顆破壞性按鈕）。舊的 `data-fav-del` 已經移除。
- **「＋ 新增常吃項目」**：不用先被 AI 估過才有，可以自己建。
- **`favPick` 與 `favQ` 都是 module-level**：這個 sheet 會重畫好幾次（切分頁、勾選、搜尋），放區域變數會被吃掉。搜尋時只換 `#fav-list` 與 `#fav-go`，不整份重畫（不然鍵盤會跳掉）。

## 今天吃得怎樣＝營養師講評（v3.4）
- **手動按才打 AI**（`data-act="coach"`，只在當天有記錄時出現）：每按一次就是一次費用，不能自動跑。
- **結果存進那一天的 day md（`## 講評` 段，v3.5）**：一天最多一則，重新評估就覆蓋。存這裡的理由是它本來就屬於那一天——跟日期綁在一起、跨裝置同步、刪掉那天就一起刪。同一天再點開不重複收費。
- **`## 講評` 必須排在 `## 備註` 之前**：備註是「進去之後不再解析 heading」的最後一段，排在它後面會被整段吃掉。
- **`cleanCoach` 是前後端鏡像**（`server.js` / `public/store.js`），陣列最多留 4 點（AI 一次噴十點會把版面塞爆）。舊的 day 檔沒有這一段照樣讀得動，有回歸測試守著。
- **看的地方就用歷史頁**，不另開分頁：評過的日子在那一列標一個 🥗，點進去就看得到；今天頁的按鈕評過之後直接顯示總評當摘要。
- **`aiCoachDay` 不共用 `aiRequest`**：system 與 schema 都不一樣（`aiRequest` 綁的是「估熱量」那組）。
- prompt 的重點是**逼它講「哪一餐的哪一樣」並給台灣買得到的替代方案**——「多吃蛋白質」誰都會講，「午餐那盤燙青菜換成滷雞腿」才照做得下去。數字先算好餵給它，不讓它自己重算。
- 會把「現在幾點、還剩多少額度」帶進去，AI 才知道要建議「接下來那一餐」還是「明天」。
- 測試：`test/browser/15-fav-coach.js`。

## 「一週約 −0.19 kg」的步調（v3.3）
- **`weekPace(gapPerDay)`＝把每天的缺口翻成一週幾公斤**（1 kg 脂肪 ≈ 7700 大卡）。「少 211 大卡」很抽象，「一週 −0.19 公斤」才有感覺——Benson 說這樣比較有動力。
- **缺口一律拿「淨攝取 vs TDEE」比，不是跟每日上限比**。上限是 TDEE 扣掉計畫缺口之後的數字，拿它算會低估實際進度（他吃 1,853 超過上限 89，但其實比 TDEE 少 211，是真的在瘦）。
- **首頁的 `paceHtml` 只在 `lateEnough`（晚上 8 點後或看過去的日子）顯示**：中午只吃 300 大卡時會算出「一週 −1.6 kg」，那是假的。歷史頁那張用 7 天平均，沒有這個問題，所以不設限。
- **黃色提示的文案跟著改成「不會胖，只是今天的缺口比計畫小」**：底下就寫著「一週 −0.19 kg」，再說「幾乎沒有減脂進度」會自打嘴巴。
- **歷史頁長條也是三段**（`mid` 黃／`over` 紅），理由同上：全部畫紅的會跟「你一週在瘦 0.18 kg」打架。
- 測試：`test/browser/14-pace.js`（含凍結時鐘測「白天不顯示」、超過 TDEE 要變 `+` 且轉紅、歷史三段配色）。

## TDEE 校準（v3.8）
- **要解決的問題**：Mifflin-St Jeor × 活動係數算的是族群平均，個人誤差常有 ±200 大卡，而 app 裡每一個數字（每日上限、一週幾公斤、圓環三段）都建在這個 TDEE 上。體重不會說謊，可以拿它反推：
  **真實 TDEE ＝ 期間平均淨攝取 ＋ 每天實際掉的體重 × 7700**。
- `calibrate()`（`public/app.js`）：往回看 `CALIB_WINDOW=28` 天，四道門檻**全過才算**，缺一項就照實說還差什麼——
  `CALIB_MIN_PTS=6`（體重筆數）／`CALIB_MIN_SPAN=13`（頭尾至少跨滿兩週）／`CALIB_MIN_LOG=10`（頭尾之間有記飲食的天數）／推算值落在 900–6,000。
  **門檻不要為了「早點看到結果」放寬**：十天內的體重是水分與肝醣在動，算出來的 TDEE 會差好幾百大卡，比公式還糟。
- **體重取最小平方法的斜率，不是頭尾相減**：頭尾兩筆各自被那天的水分決定，兩個離群值就能翻轉結論。
- **平均淨攝取只算「有記飲食」的日子**：漏記的那天不是 0 大卡、是沒有資料，算進去會把 TDEE 壓低。覆蓋率 <70% 時卡片會自己警告。
- **推算值低於 BMR 時另外警告**（`suspect`）：人不可能消耗比基礎代謝還少，那個數字幾乎一定是漏記造成的。仍然可以套用（他自己判斷），但要先講。
- **套用寫進本來就有的 `profile.tdee`（手動覆寫 TDEE）**，刻意不開第二個平行欄位——兩個 TDEE 打架時沒人知道哪個在生效。套用後卡片標「已校準」，並留一顆「改回公式估算」。
- 卡片放**歷史頁**（體重趨勢圖正下方），不放今天頁：這是幾週看一次的東西，天天佔首頁版面違反「版面整潔」。
- **今天還沒量體重時，體重卡自己變成提醒**（`.weigh.todo` 綠色虛線框），不另外加一張卡。差 ≤3 筆就湊滿門檻時文案改成「再量 N 次就能校準你的 TDEE」——比「早上量最準」有動力（Benson 的原話：他早上都忘記量）。
  **只在 `histLoaded` 之後才敢講次數**：沒翻過歷史時手上只有 7 天資料，會算出一個嚇人的數字。
- 測試：`test/browser/16-calib.js`（四道門檻各一條、反推數值、一鍵套用改動每日上限、回得去公式、體重卡提醒文案）。

## AI 估算的「同一張照片兩次差很多」（v3.9）
- **`temperature` 不是選項**：`claude-sonnet-5`／`claude-opus-5` 這一代把 `temperature`／`top_p`／`top_k` 從 API 移除了，帶進去直接回 400。**不要為了「鎖成固定答案」再去加這個參數**，只能靠 prompt 與 `effort` 收斂。
- **主要變異來源是份量判斷，不是隨機性**：照片看不出白飯幾碗、炒菜放多少油，模型每次重新想像一次份量，一個判斷就差一兩百大卡；一份便當拆成四五樣再各猜一次會放大。
- 對策一：**`AI_SYSTEM` 裡放固定的台灣份量基準表**，並要求「先判斷份量寫進 portion → 再照基準表換算」，份量落在兩值之間取中間值。把「憑印象寫總數」換成「查表換算」，前後兩次只要份量判斷一致，數字就一致。**基準表是錨點不是真理，改數字前先想清楚會連帶影響所有估算。**
- 對策二：**食物估算的 `effort` 從 `low` 提到 `medium`**（`outputConfigFor(model, schema, effort)` 第三個參數）。運動與講評維持 `low`——前者是一條 MET 公式、後者是講評，都不吃深度推理。Haiku 仍然完全不送 `effort`（會 400）。
- 對策三：AI 結果頁固定顯示 **`.ai-acc`「估算誤差約 ±20%」**。一項一項寫「652 大卡」看起來像量過的，不講清楚就會被當成 app 壞掉。**這行不是廢話，別當版面雜訊刪掉。**
- 對策四（v4.0，正解）：**吃過的東西沿用上次記的數字，根本不重估**——見下一節。上面三項只能讓它「比較穩」，這一項才能降到零。
- 測試：`test/browser/10-audit-fixes.js` 的 [D] 段釘住 Haiku 不送 effort、Sonnet 送 `medium`。

## 沿用上次記的數字（v4.0）
- **`foodKey(name)`＝食物名稱的比對 key**：去掉括號註解與標點空白（「白飯（便當盒）」→「白飯」）。**刻意不做同義詞對應**（珍奶≠珍珠奶茶）——猜太多會把不同的東西合在一起，那比多一筆更糟。`findFood(name)` 先比原字串、再比正規化。
- `rememberFood`／`starEntry`／`isStarred` **全部走 `findFood`**，不再用原字串比對；不然 AI 每次換個寫法就在常吃清單長出一筆近似重複，而且「上次記多少」永遠對不上。
- **加了星的常吃項目，`rememberFood` 不覆寫數字，只累加 `n`**：星號＝「他自己認定的固定值」（可能還手動改過），每吃一次就被新估算蓋掉的話，常吃清單永遠不會變成穩定的常數。沒加星的只是「最近吃過」的快照，照舊更新。**這段是刻意的，別「統一」成都覆寫。**
- AI 結果頁：命中常吃清單且**差 5%（或 20 大卡，取大者）以上**才提示。差幾大卡還跳出來問只是版面雜訊。
  - 上方一條 `.ai-reuse` 橫幅「有 N 樣你記過」＋一顆「全部沿用上次的數字」（一餐拆四五樣，一項一項點太煩）。
  - 每一項一顆 `.ai-last`「上次記 X 大卡（這次多 Y）· 沿用」。沿用後變成 `.ai-last.done`「已沿用」。
- `useLastFood(idx)` 會**把名稱也對齊成清單裡的寫法**，之後的比對就是完全命中，常吃清單也不會愈長愈亂。`confidence` 設 `high`（這已經不是估的了）。`it.reused` 是畫面用的暫時旗標，`cleanEntry` 不會寫進檔案。
- **刻意不自動套用**：他今天可能真的加飯，AI 這次的估算才對。自動蓋掉會靜靜地記錯，一顆按鈕的成本低得多。
- **⚠️ 沿用只借「數字」，份量文字要留今天這一次的（v6.0 修的真 bug）。**
  以前 `useLastFood` 無條件 `it.portion=f.portion`，把上一餐的情境一起搬過來——
  常吃清單裡的「荷包蛋」份量寫著「1顆，煎於蔥油餅上」（好幾天前配蔥油餅那次留下的），
  今天吃自助餐按了沿用，紀錄就變成今天也有蔥油餅，看起來像 AI 在亂猜。
  沿用的用意是「熱量跟上次一致」，不是「連那一餐的畫面一起複製」。
  AI 這次沒給份量才退回上次的。守門測試：`17-reuse.js` 的 [G] 段。
- 上游一起修：`AI_SYSTEM` 規則 **4d** 要求 portion 只寫「這樣東西有多少」，
  不要寫「煎於蔥油餅上」「被鴨肉覆蓋」「藏於海苔下方」這種**這一餐的畫面**。
  （「此次吃 3/4 份」是在講份量，可以寫。）
  這條的理由就是上面那個 bug——記過的東西會進常吃清單、下次會被沿用。
## 每日提醒＝Web Push，送出端是 GitHub Actions（v5.1）
「app 沒開的時候也要跳提醒」只有推播做得到，而**推播一定要有一台機器主動送**。
這個 app 兩台都不合格：Pages 是靜態的不會主動做事，`server.js` 外面連不到、電腦關機就沒了。
所以送的那一端是 **GitHub Actions 排程**（`.github/workflows/daily-reminder.yml`
→ `tools/send-reminders.js`）。repo 是公開的 ⇒ Actions 分鐘數免費無上限。

### 設定與金鑰（換金鑰＝所有裝置都要重新訂閱）
- VAPID 公鑰寫在 `public/app.js` 的 `VAPID_PUBLIC`（本來就要給瀏覽器，不是祕密），
  `tools/send-reminders.js` 也寫死同一份當預設值 ⇒ **要設定的只有一個 secret `VAPID_PRIVATE`**。
  兩邊的公鑰字串一致由 `test/reminders.js` 守住（不一致的症狀是推不動又沒有錯誤訊息）。
- 想覆蓋公鑰／subject 才需要另設 Actions variables `VAPID_PUBLIC`／`VAPID_SUBJECT`。
- **公私鑰是一對的**：訂閱是綁在當時那把公鑰上，換了金鑰舊訂閱一律推不動，
  而症狀是「通知就是不跳、沒有任何錯誤」。真要換就得請所有裝置重開一次提醒。

### `data/push.md`：一台裝置一行，三份 parser
- 欄位：`{id,u,time,tz,endpoint,skipIfWeighed}`＋送出後才有的 `sentAt`。
- **`tz` ＝ 瀏覽器的 `getTimezoneOffset()`（台灣 -480）**，送出端靠它把當地時間換回 UTC。
  存成字串或漏掉，症狀是**半夜跳通知**。`cleanPushSub` 會強制轉數字。
- **一台裝置 ＝ 一筆訂閱，鍵是 `endpoint`，不是 (endpoint, 使用者)**（v5.3 修的真 bug）。
  瀏覽器層面一台裝置只有一個推播訂閱，所以這是唯一正確的鍵。
  原本比 (endpoint, 使用者)，切到另一位使用者時 app 就完全看不到自己那筆——
  設定頁顯示「關閉中」、加密金鑰補不上去，而且在另一位底下按「打開」會被
  `normalizePushSubs` 的 endpoint 去重默默丟掉。守門測試 `26-push.js` 的 [J] 段。
- **`u` 的意義是「要看誰有沒有量體重」，不是「這筆屬於誰」**。綁錯人的症狀是
  「該提醒的時候不提醒」，所以設定頁會在綁定者≠目前使用者時直接寫出來，
  動任何一個控制項就改綁成目前這位。
- **同一個檔裡有所有裝置的訂閱**，寫入是整份覆蓋 ⇒
  **`savePush` 之前一定要先 `loadPush` 拿最新的整份再改**（`setMyPush` 就是在做這件事）。
  拿記憶體裡的舊清單去寫，會把女友的提醒無聲關掉。守門測試 `26-push.js` 的 [G] 段。
- `endpoint` 在公開 repo 裡看得到，但**光有它推不動**——推播服務會驗 VAPID 簽章。
- **parser 有三份**：`public/store.js`（手機寫）、`server.js`（電腦寫）、
  `tools/send-reminders.js`（Actions 讀）。任一邊分岔的症狀都是「通知就是不跳」，
  完全沒有錯誤訊息。`test/roundtrip.js` 有三方逐字比對，改格式時它會先炸。

### 刻意的決定
- **送「有內容」的推播（v5.2 改的，原本是無內容）**。v5.1 刻意送 payload-less
  省掉加密——結果 **Apple 回 201 收下了、手機卻不顯示**。payload-less 在 iOS 上是
  唯一沒把握的環節，與其猜不如消除，所以補上 RFC 8291（ECDH P-256 + HKDF + AES-128-GCM，
  Node 內建 crypto，仍然零套件）。
  - 加密要用訂閱的 **`p256dh` ＋ `auth`**，v5.1 沒存 ⇒ `loadPushState` 開 app 時
    **默默補寫回去**（不用叫使用者把提醒關掉再開）。沒有這兩把時送出端**退回無內容送法**，
    文字用 `sw.js` 裡的預設，不會整個送不出去。
  - **明文結尾一定要接一個 `0x02`**（padding delimiter），少了它瀏覽器解不開；
    症狀一樣是「收到卻不顯示」。`test/reminders.js` 會把密文解回來比對，錯了就炸。
- **`userVisibleOnly:true` 是 iOS 的硬性要求**：收到 push 一定要跳一則通知，
  靜靜吃掉瀏覽器會直接撤銷訂閱。所以「今天已經量過就不要吵」**是在送出端決定的**
  （`weighedOn()` 直接讀 repo 裡當天的 md），不是在 SW 裡判斷要不要顯示。
- **一天只吵一次靠 `sentAt`，不靠排程準時**：GitHub 排程誤點 5～30 分鐘是常態，
  尖峰甚至整次跳過。用時間窗去推一定會漏送或重送。送出後把 `sentAt` 寫回並 commit。
- **誤點超過 2 小時就跳過今天**（`WINDOW_MIN`）：早上 7:30 的提醒中午才跳只會讓人困惑。
- **410／404 ＝ 訂閱失效，直接從清單移除**；429 之類的暫時錯誤**不記 `sentAt`**，下一輪再試。
- 時間只給半小時一格（`PUSH_TIMES`）：排程 30 分鐘一次，給到分鐘級只會讓人覺得「怎麼沒準時」。

### 「通知沒跳」要看得出卡在哪一關（v5.4）
推播失敗的成因有好幾種，**從外面完全看不出是哪一種**——實際上連猜好幾輪都沒中，
每一輪都要等使用者回報，非常慢。所以設定頁直接列出這台裝置的真實狀態：

- **通知權限**（`Notification.permission`）
- **這台裝置的訂閱**（`getSubscription()` 有沒有東西）——
  ⚠️ iOS 會在「收到推播卻沒跳通知」重複發生後**自己撤銷訂閱**，
  這時雲端那筆還在、但已經推不動了，而 app 以前完全看不出來。
- **推播加密金鑰**（雲端那筆有沒有 `p256dh`/`auth`）

**狀態燈是三態，不是兩態**（v5.5）：綠＝過了、紅＝**應該要好卻壞了**、灰＝還沒開。
只有兩態的話，從來沒開過提醒的人一進來就看到兩顆紅點，會以為 app 壞了。
判斷「應該要好」的依據是**雲端有這位使用者的紀錄、或這台裝置正開著**。
壞掉時「重新設定」會從 ghost 升級成主要按鈕——那時候它才是該按的那顆。

外加一顆 **「重新設定提醒（修復用）」**（`repairPush`）：退掉舊訂閱 → 重新訂 →
整份寫回去，把上面三種情況一次收掉，不用一項一項查。

**孤兒紀錄刻意不猜著刪**：訂閱被撤銷之後 app 無從得知雲端哪一筆是自己的
（使用者可能有第二台裝置），亂刪會把別台的提醒關掉。這種紀錄由送出端收到
**410 時自動清除**——那條路本來就有。

**自動補金鑰失敗不可以整個吞掉**：v5.2 的 `.catch(function(){})` 讓「金鑰沒補上」
完全無聲，這正是查了好幾輪才找到原因的直接原因。現在會留一句話在設定頁上。

### 一台裝置一組提醒 ⇒ 兩個人要用兩支手機
`data/push.md` 的鍵是 `endpoint`，而**瀏覽器層面一台裝置只有一個推播訂閱** ⇒
同一支手機沒辦法同時有「他的」和「她的」兩組提醒，後開的會蓋掉先開的。
兩個人各自用自己的手機就沒事（各自一筆、互不影響，`26-push.js` 的 [G] 段守著）。

### 唯讀裝置要先擋（v5.6）
提醒設定要寫進 `push.md`，所以**沒貼 GitHub 金鑰的裝置開不了提醒**。
`pushBlockReason()` 會先擋——不擋的話流程很難看：通知權限問完了、訂閱也成功了，
最後才卡在存檔那一步失敗，而且那時瀏覽器已經留下一個沒人用的訂閱。
（app 其他每個寫入入口本來就有 `requireWrite` 守門，只有這裡漏了。）

### iOS 的兩條硬規則（UI 要先擋，不要給按了沒反應的按鈕）
- **只有「加入主畫面」的 PWA 收得到推播**，Safari 分頁一律不行，連權限都不會問。
  `pushBlockReason()` 會先擋下來並說明怎麼加入主畫面。
- **`Notification.requestPermission()` 必須在使用者手勢裡「直接」呼叫**。
  中間插一個 `await`（例如先去讀 `push.md`）iOS 會當成不是手勢觸發、直接拒絕——
  所以 `enablePush()` 第一件事就是要權限，其他全部往後排。
- 權限一旦被拒，瀏覽器**不會再問第二次**，只能去 iPhone 的「設定 → 通知 → 減重助手」。

### 維運注意
- **公開 repo 若 60 天沒有 commit，GitHub 會自動停用排程。** 這個 app 每天寫紀錄進 repo，
  正常用不會遇到；長期沒用要回 Actions 頁面手動啟用。
- **想立刻測一發：Actions 頁面 → Run workflow → 勾 `force`**（`FORCE=1`）。
  它會略過時間、`sentAt`、「今天量過了」三個判斷直接推，而且**刻意不寫 `sentAt`**
  ——測試不該把今天真正的提醒吃掉。設完金鑰不用等到隔天早上才知道有沒有設錯。
- 送出邏輯的測試是 `test/reminders.js`（自己起一台假的推播服務，含誤點／時區／去重情境），
  已掛進 `npm test`。app 那一端是 `test/browser/26-push.js`。

## 「懶得記」的兩個對策（v6.3）
背景（真實資料）：兩個人認真記了三週，體重反而 **+0.54 / +0.25 kg／週**。
從體重反推黏的真實 TDEE 是 **1106**——低於他的 BMR 1722，生理上不可能，
代表平均每天至少漏記 617 大卡。**真正讓他們放棄的不是麻煩，是「做了三週沒效果」**，
而那個結論本來就算得出來，只是藏在歷史頁最下面，三週從來沒被看過。
守門測試：`test/browser/29-friction.js`。

### 校準摘要上首頁（`calibBriefHtml`）
- **只在「有話要說」時出現**：`suspect`（反推低於 BMR ⇒ 漏記）、
  「體重在漲而且吃超過目標」、或 `|diff| >= 150`。其餘一律回空字串。
  每天掛一張沒有新資訊的卡片只會變成背景雜訊，跟沒有一樣。
- `suspect` 時用 **BMR 當下限**講「至少漏記多少」——這是站得住腳的最保守說法。
- ⚠️ **校準要看 28 天，但今天頁開機只載 7 天。** `ensureCalibDays()` 在背景補那 21 天，
  一次 session 只做一次。刻意不呼叫 `ensureHistory()`：那個會連 index 一起抓、載 60 天。
- ⚠️ **程式自己切 `view` 不會觸發 `ensureHistory()`**（它掛在導覽列點擊上），
  所以 `go-calib` 必須自己叫一次，不然停在「讀取紀錄中…」（測試抓到的）。

### AI 回 6 項以上預設先合併（`AI_MERGE_AUTO`、`drawAiMerged`）
- 火鍋被拆成 11 項、自助餐 5-6 項，一天 17-20 筆——這是放棄記錄的直接原因之一。
- **合併只是預設畫面，不是把資料丟掉**：「分開列出」按下去 11 項原封不動回來。
- 合併畫面刻意**不列出每一項可編輯**：會走到這裡就是因為項目太多，
  再攤開 11 個欄位等於沒解決問題。要細調就按「分開列出」。
- 名字從 `lastText`／`lastHint` 的第一個詞猜（打「火鍋」就預填「火鍋」）。
- ⚠️ 這條會影響既有測試：用 6 項以上結果的測試要先按 `[data-ai="expand"]`
  才點得到逐項的東西（`12-sync`、`13-ai-fix`、`21-tidy` 都踩過）。

## 從另一位使用者複製一餐（v6.1）
他們常常一起吃，一個人記完另一個人不用再記一次。守門測試：`test/browser/28-copy.js`。

**入口只在對方那一餐真的有「你還沒記的東西」時才出現**。實測 17 天：
早餐 14 次同時記、其中 **12 次有重疊**；午餐 14 次同時記卻**只有 1 次重疊**
（午餐各自在外面吃）。無條件顯示的話午餐會天天跳出來擋路。

- **只做「拉」不做「推」**（重要，別改）：她自己按、看得到要拿什麼、拿完才進她的紀錄。
  推（他按一下寫進她的當天紀錄）會讓她莫名其妙多出東西。**沒有人可以寫進別人的紀錄。**
- **比對用 `foodKey`**（跟常吃清單同一套）：AI 每次寫的名字會有小差異。
  已經記過的不會出現在可複製清單裡 ⇒ 複製完橫幅自動消失，不會重複加。
- **整批倍數（½／¾／一樣／1.5）**：她系統性吃得比較少，一次調完四樣比逐筆改快得多。
  倍率用 `markScaled` 記在份量欄，跟 v4.7 的單筆調倍數同一套寫法。之後仍可逐筆再調。
- **複製過去的是「她的」數字**：`rememberFood` 會把縮放後的值存進她自己的常吃清單。
  這正是不做「共用常吃清單」的理由（見上一節）——兩個人的份量本來就不一樣。
- 資料來源不用新 API：`STORE.loadDays(對方id,[date])` 兩種 store 都支援
  （手機的 PAT 涵蓋整個 repo、本機 `/api/days?u=` 本來就收任意 id）。
- **背景載入、不擋畫面**：慢的是網路，而使用者多半要去打字或拍照，橫幅晚半秒出現沒關係。
  回來時先確認餐別／日期沒被換掉（換掉就作廢），而且「本來就沒有、現在也沒有」時不重畫，
  避免每次開 sheet 都閃一下。

## 常吃清單：分組是「算」出來的，而且先把雜訊藏起來（v5.8）
需求是「清單太長，想分早餐／晚餐常吃，還要能折疊」。**但長度不是主因**——
實際資料是 114 筆裡有 **94 筆只吃過一次**（甜椒配料 3 大卡、蒔蘿與檸檬調味 5 大卡、
壽司醋飯 35 大卡…都是 AI 把一餐拆成十幾項之後被 `rememberFood` 自動存進來的碎片）。
**只做分組等於把垃圾摺起來**，所以三件事一起做。守門測試：`test/browser/27-fav.js`。

- **分組依據是新欄位 `mc`（每一餐各吃過幾次），由 `rememberFood(item, meal)` 累加。**
  **刻意不做成讓使用者手動選的分類欄**：大冰奶算早餐還是點心？手動分類一定會爛掉，
  而且沒人想維護 100 筆的分類——「他實際都在哪一餐吃」記錄裡本來就有。
  `topMeal()` 取次數最高的那一餐，平手照 `MEALS` 順序，沒資料歸「其他」。
- **`mc` 的 key 順序固定走 `MEALS`**，前後端序列化才會逐字相同（roundtrip 有測）。
  0 次的餐別不寫進檔案，不然每一筆都拖著四個 0。
- **既有資料要回填**：`tools/backfill-meals.js` 從每日紀錄重算（冪等，可重跑）。
  不回填的話升級當下所有東西都掉進「其他」，分組等於沒作用。
  ⚠️ 它的 `foodKey` 必須跟 `public/app.js` 的那份一模一樣，不然對不上那些
  「AI 每次名字有小差異」的項目。
- **預設只列「吃過 2 次以上」**（`isRegular` ＝ star 或 n≥2），其餘收在「全部」。
  他的 114 筆會變成 20 筆。**碎片沒有被刪掉**，只是不擋路。
- **搜尋一律搜全部、而且不分組**：要找的東西剛好只吃過一次是很常見的情況，
  這時候還套「只顯示常吃」的濾鏡，就變成「明明有卻找不到」。
- **`FAV_GROUP_MIN=12` 以下不分組也不過濾**：清單短的時候長度從來不是問題，
  硬要分組只會讓「三筆東西」變成「三個要點開的標題」；而且新使用者的東西
  全都只吃過一次，濾掉會看到一片空白（測試 [G] 段守著）。
- **分組預設全部收起來（v5.9，Benson 拍板）**。原本會自動展開「現在要記的那一餐」，
  但那一餐本身是照時間猜的，猜錯就變成他要多做一次收合。換餐別時**也不動**折疊狀態——
  他自己點開的那幾組要留著。重開「記一筆」才回到全收。
  ⚠️ 這條會影響別的測試：清單一旦超過 `FAV_GROUP_MIN` 就全部收起來，
  想直接點某一列的測試要先自己攤開（`15-fav-coach.js` 的 [A5] 段踩過）。

### 共用常吃清單＝審過之後刻意不做
他問過「不同帳號的常吃可以通用嗎」。**不做**，因為同一樣東西兩個人的份量不一樣：
水煮蛋 78（1顆）vs 140（兩顆）、無糖豆漿 30（120ml）vs 40（200ml）、
巧克力厚片吐司 130 vs 280。而 `rememberFood` 每記一次就會更新數字 ⇒
共用清單會讓兩個人的數字互相覆蓋，正是麻吉燒那次（v4.6）的問題重演。
真要做就是「從對方清單複製一筆過來」，各自保有自己的數字。

## 非同步流程的中斷（v5.0）— 「送出去等、然後反悔」
第三輪稽核。**這一類 bug 在截圖裡完全看不出來**，只有真的去等、然後中途反悔才會踩到。
守門測試：`test/browser/25-async.js`。抓到三個真的：

- **`runAi` 的 catch 裡寫了不存在的變數。** v4.1 把參數從 `photo` 改成複數 `photos`，
  catch 裡漏改一處 → `ReferenceError`。它在 `.catch` 裡面，所以整個 catch **從那行起中斷**、
  底下的 `drawAddSheet` 永遠不跑：AI 一失敗（沒網路、額度用完——最常走到的分支之一）
  畫面就卡在「AI 估算中」轉圈圈回不去，只能整個關掉重開。
  - **而且 console 不會紅**：它變成 unhandled promise rejection，
    **Playwright 的 `pageerror` 抓不到**。`25-async.js` 因此自己裝
    `window.addEventListener('unhandledrejection', …)`——新測試要驗非同步路徑時照做，
    不然這一類錯誤會靜靜地溜過去（這個 bug 就是這樣活了好幾版）。
- **等待中按「關閉」，AI 回來 sheet 會自己彈回來。** `.then` 無條件 `replaceSheet`，
  關掉之後它照樣長回來蓋住整個 app，導覽列點不到。
  - 解法：**sheet 堆疊每一格帶一個流水號 `sid`**（`sheetSeq`／`topSid()`／`sheetAlive(sid)`）。
    非同步回來之前先問「我當初開的那張還在最上面嗎」，不在就只 toast、不碰畫面。
    `replaceSheet` 也在堆疊已空時直接 return。
  - **`runAi`、`openCoachSheet`、`openAiItemSheet` 的重估三處都要守**；
    以後任何「先開一張等待中的 sheet、非同步回來再換內容」的流程一律照這個寫法。
  - **講評例外：畫面不彈回來，但結果照樣存進當天**（錢已經花了，別浪費）。
- **AI 失敗之後打的字被清空。** 照片的補充說明早就記在 `lastHint`，文字描述卻沒有對應的
  `lastText`——打了一長串「排骨便當加飯、湯是南瓜的…」，失敗一次就要整段重打。
  成功存檔時跟 `lastHint` 一起清掉。

### 測試自己要撐得住
壞掉的版本正是「畫面卡住／被蓋住」，這時 `click`／`inputValue` 會 timeout **直接把整支測試炸掉**，
只看得到第一個錯誤。所以 `25-async.js` 的每一段包在 `step()` 裡（catch 起來記一筆、繼續跑下一段），
取值一律用 `p.$eval(...).catch(() => null)`。在壞版本上跑會列出 9 項未過，而不是掛在第 2 項。

### 測試不可以依賴「現在幾點」
`24-ui.js` 原本點「第一列」來編輯，但列表照時間排序，前一段剛加的那筆是「現在」——
**中午前跑就會排到 12:30 那筆前面，改到別人身上去**（同一份程式碼上午紅、下午綠）。
要動特定一筆一律用名稱定位（`.row[data-act="edit-entry"]:has-text("…")`）。

## 第二輪使用者視角稽核（v4.9）
全部都是「同一件事在不同畫面長得不一樣」。守門測試：`test/browser/24-ui.js`。
- **常吃清單的熱量要帶單位**：原本只寫「900」，app 其他每一個地方都寫「大卡」。
- **手動記錄要有「份量」欄位**：常吃編輯有、AI 有，只有手動沒有。份量是這個 app 估不準的根源（見 v4.6/v4.8 兩節），而且手動記的東西也會進常吃清單，沒有份量之後就看不出「這 500 大卡是多少量」。
- **編輯一筆的「時間」欄位拿掉**：v4.5 起列表已經不顯示時間了，這個欄位變成沒有出口的孤兒；而且 `<input type="time">` 跟著系統跑成 **12 小時制（12:30 PM）**，跟 app 一律 24 小時制對不起來。**`time` 欄位本身保留**（舊資料照樣帶回來），只是不再給編輯。
- **中文句子裡的括號一律全形**：原本「熱量 (大卡)」「身高 (cm)」「體重 (kg)」是半形。`24-ui.js` 用 `/[一-龥]\s?\([^)]{1,8}\)/` 掃整頁，之後寫半形會被抓到。
- **常吃少於 8 筆時不顯示搜尋框**：短清單放搜尋只是多一個要滑過去的東西。⚠️ `15-fav-coach.js` 的 [A5] 段要先把清單湊到 8 筆以上才測得到搜尋。
- **AI 結果頁的誤差說明砍成一句**：原本三行、每次都出現，變成版面雜訊。

## 份量要用「看得到的東西」講，不要用公克（v4.8）
- Benson 的回饋，一針見血：**「我們根本不知道 AI 公克說的是多少啊，根本沒有概念」**。倍數按鈕（v4.7）預設他能拿 AI 說的公克當基準去比——但公克對他根本沒有意義，這個前提是錯的。
- `AI_SYSTEM` 規則 4c：**portion 一定要先用日常參照物描述，公克寫在括號裡。**
  - 好：「1 顆，約一顆貢丸大小（約 28g）」「一個拳頭大（約 150g）」「半碗（約 100g）」
  - 壞：「約 50g」「一份」「中份」「適量」——**使用者無法驗證的份量描述等於沒寫**。
  - 提供的參照物：雞蛋≈50g、拳頭≈一碗≈180g、掌心（不含手指）的肉≈100g、香蕉≈100g、貢丸≈22g、吐司≈30g、乒乓球≈15g、高爾夫球≈40g。
- **這是 v4.6「一顆幾公克」那條的前提條件**：逼 AI 寫出克數是為了能檢查，但克數要能被「看得到的東西」錨住，使用者才檢查得動。兩條要一起看。
- 測試：`test/browser/18-multiphoto.js` 的 [D2] 段。

## 份量的倍數調整（v4.7）— 「我又不知道它幾公克」的解法
- **人判斷「相對量」比判斷「絕對量」準得多。** 沒有人在餐桌上量得出「這一顆幾公克」，但「這比它說的少一半」是看得出來的。所以給 **×½ ×¾ ×1.5 ×2** 的按鈕，不要要求他填公克。
- 位置兩處：**編輯已記錄的那一筆**（`openEntrySheet`）與 **AI 結果頁點名稱進去**（`openAiItemSheet`）。後者刻意不做成每一列都有的 inline chips——一鍋火鍋十幾項會變成十幾排按鈕。
- **熱量與三大營養素一定要一起換算**，只改熱量會讓營養頁跟熱量對不起來。
- **份量文字不縮放、只在後面記累計倍率**（`markScaled`）：「3顆，每顆約28g，共約85g」裡的「每顆」不該跟著縮，沒有安全的縮放法。所以原文留著，後面加「（實際約0.5倍）」。連按會累乘，`×½` 再 `×2` 會回到 1 倍、標記整個消失。
- 順手修掉的不一致：`openEntrySheet` 的三大營養素以前用 `Math.round` 進位成整數（輸入框收 2.5、存檔變 3），跟手動記錄與常吃編輯兩處對不起來。改用 `round1`。
- 測試：`test/browser/23-scale.js`。

## 「一顆幾公克」是估算誤差的主要來源（v4.6，真實案例）
- 2026-08-02 同一鍋火鍋，兩個使用者各自估同一樣麻糬燒：一邊 **60 大卡一顆**、另一邊 **120 大卡一顆**。拆開來看：
  - 同一天兩人都有記到的東西，**每 100 公克的熱量幾乎完全一致**——花枝漿 133 vs 133、湯底 150 vs 150、薯條半份 180 vs 180。
  - 麻糬燒換算成密度是 214 vs 240 kcal/100g，**只差 12%**。2 倍的差距**全部**來自「一顆 28g」還是「一顆 50g」。
- **結論：AI 對「這是什麼、每 100 克多少大卡」估得很好；不準的是「你吃了多少公克」。** 之後再有人抱怨「AI 估不準」，先把 portion 換算成每 100 公克再下結論，不要直接改熱量。
- 對策寫進 `AI_SYSTEM`：
  - 規則 4a：**一顆一顆的東西，portion 一定要寫「幾顆 × 每顆幾公克 ＝ 共幾公克」**。只寫「3 顆」的話估錯了使用者也看不出來——出事的那筆就是寫「3顆，火鍋料」。
  - 明講「小顆的加工食品特別容易被高估成 50g」。
  - 新增**火鍋料的單顆重量基準表**（貢丸 20-25g、麻糬燒 25-30g、蟹肉棒 15-20g、鴨血 40-50g…）與對應的熱量密度。
- 測試：`test/browser/18-multiphoto.js` 的 [D2] 段釘住這幾條 prompt 規則。

## 全面稽核（v4.5）— 這一節是規則，不是紀錄
Benson：「可不可以不要做事做一半，我不想要花時間再去檢查這些基本的東西。」以下每一條都有 `test/browser/22-audit.js` 在守，**加新欄位／新畫面時它會先炸**。

- **`<input type="number">` 一定要寫 `step`。** 沒寫＝預設 `1`＝小數會被瀏覽器的原生驗證擋下來、按送出沒反應。而且 `inputmode` 要跟 `step` 對得起來：
  - 會有小數的（身高／體重／熱量／三大營養素）＝ `inputmode="decimal"` ＋ 明確的 `step`
  - 本質是整數的（年齡／手動 TDEE／目標缺口）＝ `inputmode="numeric"` ＋ `step="1"`
  - **最糟的組合是 `inputmode="decimal"` 沒有 `step`**——給你小數點鍵盤，又不讓你送出。身高原本就是這樣（v4.5 修）。
  - 熱量收小數但**存檔仍取整數**：0.5 大卡在 ±20% 的估算誤差裡沒有意義。收下來只是為了不跟食品標示（187.5 大卡）吵架。
- **可以點的東西一律 ≥ 44px、輸入框字級 ≥ 16px。** 之前漏掉的：`.today-btn`(32)、`.ai-last`(38)、`.ai-item .t button`(36)、`.ai-name`(36)、`.ai-nums input`(42)、`.photo-cell button`(32)。
- **鍵盤不能蓋住正在打的欄位。** iOS 的鍵盤不會把版面推上去（layout viewport 不變、只有 visual viewport 縮小），`position:fixed` 的 sheet 仍是整個螢幕高，下半部就躲在鍵盤底下。兩件事一起做才有用：`#sheet-layer` 的 `top`/`height` 綁 `--vvtop`/`--vvh`（由 `fitViewport()` 依 `visualViewport` 設定），加上 `focusin` 後延遲 320ms 的 `scrollIntoView({block:"center"})`（鍵盤有動畫，不等會捲到錯的位置）。
- **長條圖一定要畫目標線**（`goalLineHtml`）。高度是相對於「這七天的最大值」，沒有基準線的話不管吃多少最高那根都貼頂——七根滿格配上「0 天達標」的文字，**圖等於在騙人**。線的位置跟 bar 共用 `BAR_H`／`BAR_BOT`，改一個要改另一個。
- **警告要印在按鈕「之前」。** 校準卡的「推算值低於基礎代謝」原本印在「套用」下面，等於他先按了才看到。現在警告在上、而且 `suspect` 時套用鈕降級成 `ghost` 並改寫成「仍要套用」。
- **說明文字的判準（v5.7 全面精簡）：留「你要做什麼」，砍「它內部怎麼運作」。**
  砍掉的都是同一類——`data/users/…/days/*.md` 的路徑、`PWA 的殼會存在手機裡`、
  `BMR 乘上活動係數就是 TDEE`（下面的方塊已經把兩個數字都印出來了）、
  `key 不會進 GitHub`。這些是實作細節，使用者看了不會有任何不同的行動。
  **同一句話在兩個畫面各寫一次也要清掉**（活動量的「運動另外記」原本設定頁與
  建立使用者各一份、講法還不一樣）。
- **同一頁不要把同一件事講兩次。** 設定頁原本「版本」那一列寫 v4.5、頁尾又印一次「減重助手 v4.5」——頁尾那行已拿掉。
- **餐段列上不再顯示時間。** `time` 存的是「按下記錄的時間」不是「吃的時間」，中午補記早餐會在早餐底下顯示 12:30，看起來像錯的；同一批 AI 估算還會連續好幾列一樣的時間。**欄位保留**（舊資料照樣帶回來），只是不顯示。

## 長餐段摺疊、合併成一項、會長高的文字框（v4.4）
- **一餐超過 `MEAL_FOLD=4` 項就先收起來**（顯示前 3 項 ＋「還有 N 項（共 X 大卡）」）。火鍋 AI 會拆成十幾樣，整個午餐變成一整頁（Benson 截圖）。平常要看的是「這一餐吃了多少」，那個數字在餐段標題右邊本來就有。展開狀態存在記憶體的 `mealOpen`，**換日期／開別天要清掉**，不然會黏著。
- **AI 結果頁的「合併成一項」**（≥3 項才出現）：存進去之前把整批併成一筆，營養素加總、份量欄留下「共 N 樣：A、B、C…」。這是治本——今天頁只多一列，**常吃清單也只多一筆「火鍋」，下次一鍵沿用**（十幾筆各自比對沒有意義）。刻意要他自己命名，AI 猜不出這鍋該叫什麼。
- **`taHtml()` / `fitTa()` / `wireTa()`＝會自己長高的文字框**。單行 `<input>` 打超過一行，前面打的就被推出去看不到了（Benson 反映補充說明欄）。凡是「會寫成一句話」的欄位都換掉：`i-hint`／`i-text`／`m-name`／`fd-name`／`fd-por`／`fix-name`／`fix-por`／`e-name`／`mv-name`。`u-name`（限 20 字）與 `i-fav-q`（搜尋）刻意留成 `input`。
- **`wireTa()` 掛在 `drawSheet()` 裡**，所有 sheet（含 `replaceSheet` 的重畫）一次到位，各個 `onDraw` 不用各自記得呼叫。
- **`data-enter="submit"` 的欄位 Enter 仍然直接送出**，並把貼進來的換行清成空白——名稱那類欄位本來就不該有換行，這樣才不破壞「Enter 送出」那條鐵律。`i-hint`／`i-text` 沒有這個屬性（那兩個本來就該能換行）。
- 測試：`test/browser/21-tidy.js`。**`13-ai-fix.js` 原本數畫面上的列來確認「七項都寫進去」，摺疊之後會誤判**，已改成問 API。

## 體重的小數位（v4.3）
- **`step` 不是提示，是驗證**：`<input type="number" step="0.1">` 會讓瀏覽器把 59.45 判成不合法，按送出時直接被原生驗證擋下來（跳一個「最接近的有效值是 59.4 與 59.5」的泡泡）。三個體重欄位（今天的體重 `#w-val`、身體資料 `data-num="weight"`、首次設定 `#s-weight`）都改成 `step="0.01"`。**別為了「看起來乾淨」改回 0.1。**
- **`profile.weight` 以前走 `round()` 被進位成整數**（打 59.4 存進去變 59）。改用新的 `round2`。`day.weight` 本來就沒進位，但 serializer 現在也過一次 `round2`，避免奇怪的長尾小數落檔。
- **`round2` 是前後端鏡像**（`public/store.js` 與 `server.js`），跟 `round` / `round1` 同一區。改一邊要改兩邊。
- **顯示用 `kgTxt(v)`**：59 → `59.0`、59.4 → `59.4`、59.45 → `59.45`、59.456 → `59.46`。體重習慣帶一位小數，所以整數補 `.0`；但第二位有東西時要看得到，不然跟他體重計上的數字對不起來（有些是 0.05 kg 一格）。**「差多少」刻意維持一位小數**——第二位在體重的日常波動裡是雜訊。
- 測試：`test/roundtrip.js` 兩條（day 與 profile 各一）＋ `test/browser/20-weight.js`。**`01-flow.js` 原本有一條 `Number(pw) === 78` 是照著這個 bug 寫的**，已改成 78.4。

## 版本與更新（v4.2）
- **`APP_VER` 是唯一的版本來源**（`public/app.js` 最上面），設定索引、版本 sheet、頁尾那行字都讀它。**改前端時跟 `sw.js` 的 cache 版本號一起 +1。**
- 為什麼需要這個：PWA 的殼是 cache-first，新的 SW 裝好、activate 之後，畫面上跑的仍然是舊的 JS，**要重新載入才會換過去**。使用者完全看不到這件事，只會覺得「怎麼沒有新功能」。
- 偵測有兩條路，兩條都要留：`registration.updatefound` → `installing.statechange === "installed"` 且**已經有 controller**（第一次安裝不算更新），以及 `controllerchange`（同樣用進站時記下的 `hadController` 擋掉首次安裝）。`markUpdate()` 只會觸發一次。
- **設定 → 其他 → 版本**：顯示現在跑的版本、一顆「檢查有沒有新版本」（`checkUpdate()` ＝ `reg.update()` 再等 1.2 秒讓 handler 跑完）。偵測到之後索引那列改口成「有新版本 · 點一下更新」並掛黃點，sheet 換成「立即更新」（`location.reload()`）。
- **刻意不自動 reload**：記到一半被彈掉很惱人。文案有明講「記到一半的東西已經存好了」（寫入是即時的，這句是真的）。
- 測試：`test/browser/19-version.js`——**[C] 段是真的端對端**：跑到一半把 `public/sw.js` 的 cache 版本號 +1，再叫 `update()`，驗證真的偵測得到。**結束時一定會用 `finally` 把 `sw.js` 還原**，最後一條斷言就是在確認這件事。

## 一餐給多張照片（v4.1）
- **`pendingPhotos` 是陣列，上限 `MAX_PHOTOS=4`**（壓縮後每張約 1000 個 token，四張還在零頭；再多只是讓 AI 更容易重複計算）。相簿的 input 帶 `multiple`，相機的不帶。
- **多張時 prompt 一定要有「同一樣東西只能算一次」**：一餐分好幾盤要各自列出來，但同一盤換角度拍的兩張若被當成兩份，熱量會直接翻倍。**這句話不能為了精簡刪掉**，`18-multiphoto.js` 有釘住。單張時刻意不講（沒有重複的問題，講了只是雜訊）。
- `photoBlocks(photos)` 同時吃陣列與單一字串；壞掉的圖直接略過，不讓一張壞圖擋掉整批。
- 多張照片**一張一張接著壓縮**（`reduce` 串成 promise chain），不要 `Promise.all`——手機上同時解好幾張大圖很容易把記憶體吃爆。
- `aiResult.photos` 是陣列（原本是單數 `photo`）：「補充說明整批再估一次」與「重估其中一項」都會把**所有**照片一起帶回去。
- 縮圖列一張時滿版、兩張以上排成兩欄方形（`.photo-strip.multi`），每格右上有移除鈕。
- 測試：`test/browser/18-multiphoto.js`。

- 測試：`test/browser/17-reuse.js`（第一次沒得沿用、第二次認得出來並講得出差多少、一鍵沿用回到同一個總數、5% 以內不吵、括號寫法也認得、加星的不被覆寫）。

## AI 結果的單筆修正（v3.2）
- **`photoPickHtml()` 必須把已選的 `pendingPhoto` 畫回來**。這一頁會因為切分頁、AI 估完退回等原因重畫好幾次，而照片一直都在記憶體裡；以前固定畫空狀態＋送出鈕 disabled，看起來就像照片不見了，使用者只好重拍（Benson 的原始抱怨其實是這個 bug）。`lastHint` 同理，退回去補充說明不用重打。
- **`aiResult.photo` 記住這批結果是哪張照片估的**（`runAi(fn, photo)` 帶進來），單筆重估要把同一張圖再送一次——使用者最常遇到的是「東西認錯了」（叉燒被當成燒鴨），光靠文字 AI 看不到真正的份量。
- **`aiAnalyzeOne(model, photo, wrongName, said)`**：prompt 明講「其他項目不用列」，回來用 `splice(idx,1,...items)` 只換掉那一項。刻意允許回多項（一句話裡其實有兩樣東西時）。
- 結果頁每一項的名稱是按鈕（`data-fix="<idx>"`），`data-fix="-1"` ＝「＋ 補一項」。次要按鈕 `data-fix2`：`rename`（只改名稱不打 AI）、`manual`（先加 0 大卡，數字自己填）。
- 測試：`test/browser/13-ai-fix.js`（照片留著、只打一次 AI、請求含原圖與「原本判斷成什麼」、其他項原封不動）。

## 生日 `birth`（v3.0，profile 新欄位）
- **格式 `YYYY-MM-DD`、選填**，序列化在 `age` 之後（前後端欄位順序是鏡像，改要一起改）。
- **填了生日之後 `age` 一律由它推算**：`cleanProfile` 在每次讀檔／存檔都跑 `ageFromBirth`，所以生日一過年齡自己 +1，BMR／TDEE 跟著更新，不用每年手動改。使用者自己填的 age 會被蓋掉。
- **`cleanBirth` 擋掉**：格式不合、不存在的日期（2-30）、1900 年以前、未來日期 —— 一律回 `""`，那時候才沿用自己填的 age。
- **沒填生日的人維持原本自己輸入年齡**（不強迫交出生日）。UI：有生日時年齡欄變成唯讀的 `.static-val`「30 歲 · 生日到了會自動加」，清空生日就變回可輸入的數字欄。
- **舊 profile 檔沒有 `birth` 這行照樣讀得動**（parser 回 `""`），有回歸測試守著。
- **⚠️ 生日欄的 change 事件裡絕對不能重畫整個 sheet**：iOS 原生日期選單只要那個 `<input>` 被換掉就會立刻關閉——使用者才滾完「年」就被關掉、而且值已經套用了（v3.0 出過這個 bug）。只換衍生出來的三小塊：`#age-field`、`#birth-hint`、`#set-live`，日期欄本身完全不動。年齡欄也只在「可輸入 ↔ 唯讀」真的要換型態時才重建。
- **`blur` 只落檔、不重畫**：blur 發生在焦點移到下一個欄位的當下，這時動 DOM 會把使用者正要輸入的欄位抽掉。
- **落檔要延後（900ms debounce）**：滾一次年份就 change 一次，手機上每次寫入＝一個 GitHub commit。
- 測試：`test/roundtrip.js`（生日推算、無效值、舊檔相容、兩邊算出來的 age 逐字相同）、`test/browser/11-settings.js`[3b][3c]（含「滾年份後日期欄必須是同一個元素、焦點還在、還沒落檔」的回歸斷言）。

## 營養頁（v2.9 精簡後，別加回去）
- **三大營養素合成一張卡 `.mcard`**（本來三張獨立卡各佔 270px，光這裡就要滑一屏半）。整頁現在一屏看得完。
- **蛋白質不足的建議直接接在那一列的 `.mrow-foot`**，不再另開一張 `.tip` 黃卡——那等於同一件事講兩次（列裡已經寫「差 13 g」）。
- **拿掉「熱量來源」的三色比例條**：Benson 直接反映看不懂，而且三列的數字已經隱含比例。它唯一有用的「三大加起來 X 大卡 vs 記錄 Y 大卡」落差提醒保留下來（只在差 >15% 時出現，代表有幾筆只填熱量沒填營養素）。
- **每列的「目標＝…」公式收進最下面的「目標是怎麼算的」展開（`showMNote`）**：公式看一次就懂，天天佔版面是雜訊，設定頁也寫過。
- **7 天圖的主標是「平均 X g／目標 Y」，達標天數降級成小字**：每天都差一點時「0/7 天達標」當標題很打擊人，但平均值才是真正該看的距離。
- 測試：`08-macros.js`[4][5][8]（含「不該有 .tip / .split」的反向斷言）。

## 今天頁的餐段清單（v2.8）
- **只列「有記東西」的餐段**，空的收成最下面一列「還沒記」的 chips（含運動）。以前四餐＋運動不管有沒有東西都各佔一張卡，空白的一天要滑過五張「＋ 記一筆◯◯」。
- **每一筆固定一行**：`.row-mid span` 是 `nowrap + ellipsis`。AI 的份量說明常常很長（「約一碗份量(含飯約200g熟重、綜合菇類、起司醬汁)」），會把列撐成兩行、高矮不一；完整內容點進去看得到。
- 測試：`01-flow.js`[G0]。

## 首頁圓環＝三段（v2.5，別改回兩段）
- 綠＝還在減脂上限內／**黃＝超過減脂上限但還沒超過 TDEE**／紅＝超過 TDEE。
- 中間那段是重點：「每日上限」是 TDEE 扣掉缺口之後的數字，超過它只代表**這天沒有減脂進度**，不代表會胖。以前跟「真的超過 TDEE」畫成同一種紅色，Benson 自己就誤讀過（吃 2,288、運動 240、淨 2,048 vs TDEE 2,064，實際是打平，畫面卻紅字寫「超過 284」）。
- 提示條文案也分兩種：黃＝「超過上限 X · 距離 TDEE 還有 Y／今天大致是維持體重，不會胖，但也幾乎沒有減脂進度」；紅＝「超過上限 X · 已超出 TDEE Y／超出 TDEE 的部分才是真正會變胖的量」。
- 圓環右邊在減脂模式下多列一行 **維持體重（TDEE）**，讓「上限」看得出是從哪來的。目標設成維持／增肌時不顯示（target 就是 TDEE，列了是重複）。
- 測試：`test/browser/09-floor.js` 的 [5]／[5b]／[5c]。

## 設定頁＝索引式（v2.4 改版，別攤回一頁）
- 一列一個主題（`.set-row`，右邊直接寫**目前值**），控制項全部收在各自的 sheet 裡（`openSettingsSheet(sec)`）。原本一頁六張大卡全攤開，手機上要滑很久才找得到東西。
- **索引與 sheet 的資料來源是同一份 `setSections()`**，摘要別在別的地方各寫一次。
- **例外：警示留在索引上**（`.set-alert` ＋ 那一列的 `.wdot`）。「上限低於 BMR／缺口佔 TDEE 超過 25%」是不該要他自己翻進去才看得到的資訊，點警示直接開「每日目標」。
- **數字欄位（`data-num`）改值時只換 `#set-live` 這一塊**，不整份重畫——重畫會把正在編輯的 input 換掉，手機鍵盤會跳掉。chip（`data-set`）才做整份重畫（選中狀態要更新）。
- 從 sheet 裡按「切換使用者／新增使用者」要先 `closeAllSheets()`，不然選人畫面會被 sheet 蓋住。
- 測試：`test/browser/11-settings.js`。其他測試要動設定，用 `_setup.js` 的 `openSet(page, sec)` / `closeSet(page)`。

## PWA 鐵律（travel-book 血淚，全部已做，別退步）
- 所有資源、manifest `start_url`/`scope`、SW scope **一律相對路徑**（Pages 在 `/lose-weight-helper/` 子路徑）。
- SW：`skipWaiting()`＋activate 清舊快取＋`clients.claim()`；GET `/api` network-first、寫入 network-only、殼 cache-first；**跨網域（Anthropic／GitHub）直接放行不攔**。**改前端記得把 sw.js 的 cache 版本號 +1**（`lwh-shell-vN`，目前 v1）。
- input/textarea/select `font-size ≥ 16px`（iOS 防自動放大）；觸控目標 ≥ 44px；Enter 送出全部走原生 `<form>` + `type=submit`。
- **`#sheet-layer[hidden]{display:none;}` 這行不能省**：`#sheet-layer` 的 ID 選擇器優先度高於瀏覽器對 `[hidden]` 的 `display:none`，少了它 sheet 關掉後仍是一層看不見的全螢幕遮罩，**整個 app 都點不動**（已踩過，實測抓到）。
- 換 icon 後 iOS 已安裝的 PWA 要移除主畫面重加才會換。

## 其他實作備忘
- **`output_config.effort` 只在推理型模型送**（`outputConfigFor`）：Haiku 4.5 收到會直接回 400，雖然有純 JSON 降級路徑接住，但等於每次都白打一次請求（慢、又多算一次 usage）。新增模型時記得確認這個開關。
- 常吃清單支援**刪單筆**（`data-fav-del`，有 confirm）：AI 偶爾會生出「甜椒配料 3 大卡」這種項目，只有「清空全部」的話會一直卡在清單裡。
- 寫入用 per-檔案 promise chain 排隊（`persistChains`），避免快速連點時並發互蓋。
- 樂觀更新：畫面先動、背景寫入，失敗才 toast。
- 唯讀模式（Pages 無 PAT）：所有寫入動作入口都有 `requireWrite()` 守門＋toast 提示。
- 日期一律用**當地時區**的 `dateKey()`，**不可以用 `toISOString()`**（那是 UTC，台灣半夜會跳成前一天）。
- PWA 常常整天不關：`visibilitychange` 會在過午夜後把 `curDate` 推到新的一天。
- icon 產生工具在 scratchpad、**不進 repo**；server.js 保持零執行期依賴。
- 熱量環在 0 時**不畫彩色弧**：`stroke-linecap:round` 會在 dasharray 0 的位置留一個小圓點。
- **AI 回來的項目要過 `isRealFood` 濾一次（v6.2）**。實機截圖抓到：AI 在真的那筆之外
  多吐一筆 `name` 叫 **`note_placeholder`**、四個數字全 0、沒有份量的填充項目，
  畫面上就多一張卡片要使用者自己按 ✕。
  - **⚠️ 判準不能只看「熱量 0」**：無糖麥茶、無糖茶這種真的就是 0 大卡、0 營養素。
    用「四個數字全 0 **而且**連份量都沒有」——真的食物一定有份量（schema required）。
  - 另外擋掉「看起來像程式變數名」的名稱（含底線的純英數）。**刻意要求有底線**：
    Oreo、Subway 這種純英文食物名沒有底線，不能一起砍掉。
  - 濾完變空的話走「沒認出食物」那條錯誤，不要給一張空的結果頁。
  - 守門測試：`13-ai-fix.js` 的 [G] 段（在修好前的版本上會重現截圖裡的「加入 2 筆 · 共 270 大卡」）。
- **AI 有降級路徑**：`output_config` 的形狀若被 API 回 400，會自動改用「提示詞要求純 JSON」再試一次（`aiRequest(..., plain)`）。
  沒有 API key 就無法驗證 structured outputs 的形狀，這條保險是刻意留的，別當多餘刪掉。
- **照片有兩個 file input，這是刻意的**：`capture="environment"` 會**強制**開相機，手機上就完全看不到相簿；
  不帶 `capture` 才會讓系統給選單。與其靠系統選單，不如直接給「拍照」「從相簿選」兩顆按鈕。
  **不要把兩個 input 合併成一個**，合併就一定得犧牲其中一種來源。
- 每次處理完照片要把 `input.value=""` 清掉，否則使用者連選同一張時 `change` 不會再觸發。
- **照片走 `createImageBitmap({imageOrientation:"from-image"})`**：iPhone 直向拍的 EXIF orientation 常是 6，
  用 `<img>`+canvas 不會轉正，會把躺著的照片送給 AI，判讀變差。舊瀏覽器才退回 `<img>` 路徑。
- 測試：`npm test`（`test/roundtrip.js`）＋ `npm run test:browser`（`test/browser/`，需要 `npm i -D playwright`）。
  `test/browser/README.md` 有寫**哪些是假的外部服務、哪些還沒驗過**——接手前先看那一段。裡面有一條**「前端 store.js 的 serializer 產出與 server.js 逐字相同」**——這是防止前後端 mirror 無聲分岔的主要保險，改格式時它會先炸。

## 啟動
- 雙擊 `start.bat`（只跑 `node server.js`，port 3619）。
- **server.js 啟動時自己執行 build 鏡射 docs/**（build.js 匯出 `build()`），所以從任何入口啟動 docs/ 都不會落後 public/；build 失敗只記 log 不擋服務。
